package com.owlmetry.android.compose

import androidx.compose.runtime.Composable

/**
 * Phase 1 scaffold placeholder so the Compose module compiles and the Compose
 * compiler plugin is wired. Replaced by `OwlFeedbackView` (Phase 7) and
 * `OwlQuestionnaireView` + `owlScreen` (Phases 8–9).
 */
@Composable
internal fun OwlComposePlaceholder() {
}
